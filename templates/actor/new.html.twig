{% extends 'base.html.twig' %}

{% block title %}New Actor{% endblock %}

{% block body %}
<div class="container">    
    <h1>Create new Actor</br>
    <a class="btn btn-link" href="{{ path('actor_index') }}">Back to list</a></h1>

    {{ include('actor/_form.html.twig') }}
</div>
{% endblock %}
